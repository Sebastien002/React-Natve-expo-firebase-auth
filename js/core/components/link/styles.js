import React from "react-native";

var styles = React.StyleSheet.create({
    container: {},
    text: {
        color: '#777',
        fontSize: 14,
        fontWeight: 'bold'
    }
})

export default styles
