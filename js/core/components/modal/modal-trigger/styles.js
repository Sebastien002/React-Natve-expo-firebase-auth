import React from 'react-native';
var styles = React.StyleSheet.create({
    close: {position: 'absolute', top: 15, right: 15},
    closeIcon: {color: '#e6e6e6', fontSize: 70}
});

export default styles;
