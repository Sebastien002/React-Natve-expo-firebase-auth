/**
 * This whole package encapsulates the routing for the app
 * regardless of which routing architecture or plugin is used
 */

export {
    default as Navigator, INITIAL_ROUTE, INITIAL_UNSECURED_ROUTE, INITIAL_SECURED_ROUTE
} from './routes';
